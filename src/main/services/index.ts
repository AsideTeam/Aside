/**
 * Services 초기화
 */

export async function initializeServices(db: any): Promise<void> {
  // 필요한 서비스들을 여기서 초기화
  // e.g., AdBlockService.initialize(), HistoryService.initialize() 등
  
  // 현재는 빈 상태 (Phase 2에서 채움)
}
