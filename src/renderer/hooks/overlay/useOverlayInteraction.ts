/**
 * useOverlayInteraction - Redundant (Metrics managed by components)
 *
 * Note: Metrics are now sent directly by Sidebar.tsx and AsideHeader.tsx.
 * This hook is kept as a stub to avoid breaking imports.
 */
export function useOverlayInteraction(): void {
  // Do nothing. Metrics are handled by components.
}
