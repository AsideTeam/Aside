/**
 * SidebarFooter Component
 *
 * 사이드바 하단 푸터 (다운로드/추가) - 현재 미사용
 * HTML에서 직접 렌더링됨
 */

import React from 'react'

interface SidebarFooterProps {
  _onDownloadsClick?: () => void
  _onAddClick?: () => void
}

export const SidebarFooter: React.FC<SidebarFooterProps> = () => {
  return null
}
